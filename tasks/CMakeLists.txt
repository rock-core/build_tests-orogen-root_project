# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(root_projectTaskLib)
ADD_LIBRARY(${ROOT_PROJECT_TASKLIB_NAME} SHARED 
    ${ROOT_PROJECT_TASKLIB_SOURCES})
add_dependencies(${ROOT_PROJECT_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${ROOT_PROJECT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${ROOT_PROJECT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ROOT_PROJECT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ROOT_PROJECT_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${ROOT_PROJECT_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${ROOT_PROJECT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ROOT_PROJECT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    ARCHIVE DESTINATION lib/orocos
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ROOT_PROJECT_TASKLIB_HEADERS}
    DESTINATION include/orocos/root_project)

